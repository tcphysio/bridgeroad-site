{
  "name": "bridgeroad-site",
  "private": true,
  "description": "Bridge Road Physiotherapy website. Static pages plus Vercel Functions in api/. No build step.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
