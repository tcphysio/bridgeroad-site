{
  "name": "bridgeroad-site",
  "private": true,
  "description": "Bridge Road Physiotherapy website. Static pages plus Vercel Functions in api/. One deploy step: tools/stamp-assets.js.",
  "scripts": {
    "build": "node tools/stamp-assets.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
